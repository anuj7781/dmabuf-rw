From b83e184fc3d727639e9ee4ac56559b998e470b38 Mon Sep 17 00:00:00 2001
From: Anuj Gupta <anuj20.g@samsung.com>
Date: Thu, 10 Sep 2026 15:54:10 +0530
Subject: [PATCH 16/16] dma-buf: defer percpu_ref_exit() to the map's final
 release

A queued request can retain a software map reference after unmap and
later attempt to acquire the active reference. Keep the percpu_ref
initialized until the map's final software reference is released.

Signed-off-by: Anuj Gupta <anuj20.g@samsung.com>

diff --git a/drivers/dma-buf/dma-buf-io.c b/drivers/dma-buf/dma-buf-io.c
index a52386cdc964..ca248cd1c5f5 100644
--- a/drivers/dma-buf/dma-buf-io.c
+++ b/drivers/dma-buf/dma-buf-io.c
@@ -57,6 +57,7 @@ static void dma_buf_io_map_free_rcu(struct rcu_head *rcu)
 	struct dma_buf_io_map *map = container_of(rcu, struct dma_buf_io_map, rcu);
 	struct dma_buf_io_ctx *ctx = map->ctx;
 
+	percpu_ref_exit(&map->active);
 	kfree(map);
 	dma_buf_io_ctx_put(ctx);
 }
@@ -82,7 +83,6 @@ static void dma_buf_io_map_release_work(struct work_struct *work)
 	dma_resv_unlock(dmabuf->resv);
 
 	dma_fence_put(fence);
-	percpu_ref_exit(&map->active);
 	kref_put(&map->refs, __dma_buf_io_map_free);
 }
 
@@ -205,7 +205,6 @@ static void dma_buf_io_drop_map(struct dma_buf_io_ctx *ctx)
 		ctx->dev_ops->unmap(ctx, map);
 		kfree(map->fence);
 		map->fence = NULL;
-		percpu_ref_exit(&map->active);
 		kref_put(&map->refs, __dma_buf_io_map_free);
 		return;
 	}
-- 
2.43.0

